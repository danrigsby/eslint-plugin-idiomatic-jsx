machine:
  node:
    version: 7

dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
    - yarn
  override:
    - yarn run build

deployment:
  npm:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - yarn publish
